spring:
  application:
    name: eureka-registry
  cloud:
    config:
      name: eureka-registry
      uri: http://config-server:8888
      fail-fast: true

eureka:
  instance:
    #服务注册中心实例的主机名
    hostname: 10.112.164.121
  client:
    #是否检索服务
    fetch-registry: false
    #是否向服务注册中心注册自己
    register-with-eureka: false
